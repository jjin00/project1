package bbGame;

import java.util.List;

public class PlayGame {
    public static void main(String[] args) {
        CreateRandom random = new CreateRandom(); //컴퓨터가 생성하는 랜덤값 (hashset)
        UserInput input = new UserInput(); // user가 입력하는 입력 값
        CompareNum compare = new CompareNum(); // user입력값과 컴퓨터값 비교
        GameCtrl ctrlGame = new GameCtrl();
        boolean retry = true;

        while (retry){
            hashset random = random.setNum();
           
            }
         //   retry = 
        }
    }
